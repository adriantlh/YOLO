# Copy to .env and edit values as needed, then `source .env` before starting.

# Optional: custom model config
# YOLO_MODEL_CFG=yolo/config/model/v9-c.yaml

# Path to your trained weights (.pt). If omitted, model runs without pretrained weights.
# YOLO_WEIGHT_PATH=weights/your_model.pt

# Path to dataset yaml providing class names (names: [...]) for readable labels.
# YOLO_DATA_YAML=data.yaml

# Server networking (optional)
# HOST=0.0.0.0
# PORT=8000

